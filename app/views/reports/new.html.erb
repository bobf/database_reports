<%= turbo_frame_tag :main do %>
  <%= turbo_flash_messages %>
  <% if database_configured? %>
    <div class="container">
      <div class="container">
        <%= h1('New Report') %>
      </div>
      <div class="container">
        <%= render partial: 'reports/form' %>
      </div>
    </div>
  <% else %>
    <p>You must configure a database before creating a report.</p>
  <% end %>
<% end %>
