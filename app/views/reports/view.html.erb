<% if @error.present? %>
  <%= render partial: 'shared/error', locals: { report_url: report_path(@report) } %>
<% else %>
  <%= render partial: 'shared/report', locals: { report_url: report_path(@report) } %>
<% end %>
