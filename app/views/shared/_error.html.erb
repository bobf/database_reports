<div class="error container">
  <%= render partial: 'shared/summary', locals: { report_url: report_url } %>

  <h1 class="font-medium leading-tight text-4xl mt-0 mb-2">Error executing query</h1>
  <p class="text-gray-500 font-mono py-3"><%= @report.query %></p>
  <p class="text-red-500 font-mono py-3"><%= @error %></p>
</div>
